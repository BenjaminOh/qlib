# 신호 점수 붕괴(퇴화) 근본 원인 진단 — 2026-08-05

> 실행: `scripts/diagnose_collapse.py` (10:01~10:03, worker 컨테이너, 4일 × 2변형).
> 대상: 정상일 7/30·7/31 vs 퇴화일 8/3·8/4 (각각 as_of 7/31·8/3 정상 신호, as_of 8/4·8/5 퇴화 신호 생성일).

## 결론 (한 줄)

**H1 조기종료 기아 확정.** 검증 손실이 사실상 소음 수준에서 요동하고, 퇴화일에는 부스팅
1~6회 만에 조기종료가 걸려 **트리 1~6개짜리 모델**이 만들어진다. 트리 1개 = 잎 ~30개 →
183종목이 6가지 점수로 뭉개짐. 라벨·피처·데이터는 전부 정상.

## 가설별 판정

| 가설 | 판정 | 근거 |
|------|------|------|
| **H1 조기종료 기아** | ✅ **원인** | best_iteration: 7/30 **24** · 7/31 **24** · 8/3 **6** · 8/4 **1**. 8/4는 1회차 이후 검증 손실이 단조 상승 → 트리 1개(분기 30개)로 종료. 점수 고유값 6개/183종목 |
| H2 라벨 퇴화 | ❌ 기각 | train/valid 라벨 NaN 0%, std 0.997로 4일 모두 동일. 마지막 1~2일 라벨 NaN은 구조적(미래 종가 미존재)이고 매일 같음 — DropnaLabel이 정상 제거 |
| H3 피처 동결/중복 | ❌ 기각 | 4일 모두: 분산 0 피처 0개, 완전 동일 피처행 0쌍, NaN 0.6%대 |
| H4 시장 동조화 | ❌ 기각(주원인 아님) | 일자별 횡단면 라벨 std 0.024~0.074 범위에서 정상 요동 — 퇴화일 직전에 급락 없음 |

## 심층 관찰 (수치)

| 훈련일 | best_iter | 실질 트리 | 전체 분기 | 점수 고유값(185~183종목) | 최빈 점수 점유율 | 점수 std |
|--------|-----------|----------|----------|--------------------------|------------------|----------|
| 7/30 (정상) | 24 | 24 | 720 | 14 | 47.6% | 0.00214 |
| 7/31 (정상) | 24 | 24 | 720 | 35 | 34.1% | 0.00630 |
| 8/3 (퇴화) | 6 | 6 | 180 | 11 | 41.6% | 0.00412 |
| 8/4 (퇴화) | **1** | **1** | **30** | **6** | 39.3% | 0.00072 |

핵심 통찰 2가지:

1. **"정상일"도 사실 뭉쳐 있다.** 7/30조차 185종목 중 절반(47.6%)이 같은 점수, 고유값 14개.
   top-10이 우연히 서로 다른 잎에 앉아 "고유값 10"으로 보였을 뿐. 우리가 게이트로 삼는
   top-10 고유값은 **운에 좌우되는 취약한 지표**였다.
2. **검증 손실의 개선폭이 소음 수준.** 모든 날 valid l2 ≈ 0.9946에서 시작해 최저점도
   0.9943~0.9946 (개선 0.03% 미만, CSZScoreNorm 라벨 기준 설명력 ~0.5%). best_iteration은
   이 미세한 요동의 "운"으로 결정된다 — 운이 나쁘면(8/4) 1회 만에 종료.

## 치료 후보 검증: valid6m (검증 창 3→6개월)

❌ **효과 없음 — 채택 불가.** 8/4에 동일하게 best_iter 1. 심지어 정상일(7/30·31)엔 9회로
프로덕션(24회)보다 오히려 나빴다. 검증 창을 늘려도 "개선폭이 소음"이라는 본질이 안 바뀜.

## 대응: 고정 라운드 학습 채택 (2026-08-05 사용자 승인, 당일 배포)

**사전 검증 (remedy_fixed150 — lr 0.005 · 150라운드 · 조기종료 완전 제거):**

| 훈련일 | 트리 수 | 점수 고유값 | 최빈 점유율 | top-10 고유값 |
|--------|--------|------------|------------|--------------|
| 7/30 | 150 | **145**/185 (기존 14) | **4.3%** (기존 47.6%) | **10/10** (기존 2) |
| 7/31 | 150 | **136**/185 (기존 35) | 5.4% | **10/10** (기존 7) |
| 8/3 (퇴화일) | 150 | **130**/185 (기존 11) | 5.9% | **10/10** (기존 3) |
| 8/4 (퇴화일) | 150 | **144**/183 (기존 6) | 5.5% | **10/10** (기존 3) |

4일 전부(퇴화일 포함) top-10 완전 분화. 검증 손실 악화는 최대 +0.2%로 수용 범위.

**구현 함정 (중요)**: lightgbm의 early_stopping 콜백은 patience를 아무리 키워도
학습 종료 시 best_iteration을 강제 기록하고, predict/dump가 그 시점까지만 사용한다
(1차 시도에서 150라운드를 돌리고도 결과가 기존과 비트 단위로 동일했던 이유).
→ **콜백을 아예 제거해야 함**: 포크의 `qlib/contrib/model/gbdt.py` fit()을
`early_stopping_rounds=0 → 콜백 미등록`으로 패치, `live_trader.py LIVE_CONFIG`를
`{"learning_rate": 0.005, "num_boost_round": 150, "early_stopping_rounds": 0}`로 변경.

**미채택 대안**: 선형 ε-블렌드(동점 해소 — fixed150로 불필요해짐), 동점일 매수 보류
가드(방어책 — 재발 시 재검토), ~~valid6m~~(기각).

**관찰 계획**: 8/5 저녁 신호부터 적용. 일일 점검에서 unique_scores(top-10)와 함께
전체 횡단면 n_unique·mode_share 추적 — fixed150 기준 정상 범위는 n_unique 130+,
mode_share <10%.

## 부가 사실

- 스코어링 유니버스는 KOSPI200 구성 ~183-185종목 (2,000종목 아님 — 상장 전체가 아니라
  kospi200 인스트루먼트 기준).
- 기존 top-10 고유값 게이트(5거래일 연속 ≥5)는 지표 자체가 취약 — 전체 횡단면
  `n_unique`/`mode_share`(진단 스크립트 산출)로 교체 검토 권장.
